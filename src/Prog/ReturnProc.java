package Prog;

import Entities.Character;

public class ReturnProc extends Action {
	public ReturnProc() {} //Sert à être identifier dans l'interpréteur
	
	public boolean execute(Character p) {return false;}
}
